import { createApp } from "vue";
import Bheader from "@/components/Bheader.vue";

// import TaskContainer from '@/components/TaskContainer.vue';

createApp(Bheader).mount("#header");
// 掛載到app上
// createApp(TaskContainer).mount("#app");
